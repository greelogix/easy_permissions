name: easy_permissions
description: A JSON-based Flutter permission manager for Android, iOS, and Web.
version: 0.0.1
homepage: https://github.com/example/easy_permissions

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.0.0
  xml: ^6.3.0
  args: ^2.4.2
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
